Vendor Portal Backend (Laravel overlay)
Drop these files into a Laravel project root (merge with existing files).
Run: composer require tymon/jwt-auth, php artisan jwt:secret, php artisan migrate --seed
